apiVersion: v1
kind: Service
metadata:
  name: chat-service
  namespace: zalord-staging
spec:
  selector:
    app: chat-service
  ports:
    - name: http
      port: 8084
      targetPort: http
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: chat-service
  namespace: zalord-staging
spec:
  replicas: 1
  selector:
    matchLabels:
      app: chat-service
  template:
    metadata:
      labels:
        app: chat-service
    spec:
      containers:
        - name: chat-service
          image: docker.io/${DOCKER_NAMESPACE}/zalord-chat-service:${IMAGE_TAG}
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 8084
          envFrom:
            - configMapRef:
                name: chat-service-config
            - configMapRef:
                name: shared-config
            - secretRef:
                name: shared-secret
          env:
            - name: MQ_URI
              value: "amqp://$(RABBITMQ_USER):$(RABBITMQ_PASSWORD)@$(MQ_HOST):$(MQ_PORT)/"
            - name: OTEL_EXPORTER_OTLP_ENDPOINT
              value: "http://otel-collector:4317"
            - name: OTEL_SERVICE_NAME
              value: "chat-service"
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 10
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 20
            periodSeconds: 15
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 250m
              memory: 128Mi
